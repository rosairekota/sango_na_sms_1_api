import { AntigenefemmeEntity } from './antigenefemme.entity';

describe('AntigenefemmeEntity', () => {
  it('should be defined', () => {
    expect(new AntigenefemmeEntity()).toBeDefined();
  });
});
