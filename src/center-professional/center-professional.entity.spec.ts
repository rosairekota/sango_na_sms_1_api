import { CenterProfessionalEntity } from './center-professional.entity';

describe('CenterProfessionalEntity', () => {
  it('should be defined', () => {
    expect(new CenterProfessionalEntity()).toBeDefined();
  });
});
