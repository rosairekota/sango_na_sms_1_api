import { ProvincialProfessionalEntity } from './provincial-professional.entity';

describe('ProvincialProfessionalEntity', () => {
  it('should be defined', () => {
    expect(new ProvincialProfessionalEntity()).toBeDefined();
  });
});
